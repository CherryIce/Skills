# {{APP_NAME}} 功能审核报告

审核日期：{{RUN_DATE}}

独立审核结论：`not_evaluated`。本报告必须说明证据层，不得把静态、测试、模拟器或 Archive 结论扩大到真机或 App Store。

## Findings

| 优先级 | 需求/流程 | 发现 | 用户影响 | 证据 | 修复验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| [P0–P3] | [FR/流程] | [填写] | [填写] | [文件/测试/运行态] | [可验证条件] | open/closed |

若无发现，明确写“未发现开放 P0/P1”，同时保留未测试层。

## 核心闭环

| 环节 | 结论 | 证据与边界 |
| --- | --- | --- |
| 输入/取消/失败 | `not_evaluated` | [填写] |
| 核心处理 | `not_evaluated` | [填写] |
| 用户确认/撤销 | `not_evaluated` | [填写] |
| 自动复检或状态重算 | `not_evaluated` | [填写] |
| 持久化/恢复/删除 | `not_evaluated` | [填写] |
| 输出/分享 | `not_evaluated` | [填写] |
| 中英文/深色/响应式/无障碍 | `not_evaluated` | [填写] |
| 离线/隐私边界 | `not_evaluated` | [填写] |

## 审核发现与关闭记录

1. [记录第一轮问题、修复、回归测试和关闭证据]

## 自动化结果

引用 `flutter_quality_gate.py` 生成的 Markdown/JSON 和原始日志；不得手工伪造通过数量。

## 未测试层

- 实体设备、最低系统、低内存/低存储、后台中断：[填写]
- 真实系统选择器、分享目的地、通知或其他平台集成：[填写]
- VoiceOver/TalkBack、完整 Dynamic Type、横屏和视觉矩阵：[填写]
- 签名 Release、TestFlight、App Store/Play 审核：[填写]
